#include "client.hpp"

/*void orders(int i,char * buffer, int bufsize)
{    
    if (strncmp("NICK ", buffer, 5))
    {
        nickname();
    }
    memset(buffer,0,bufsize);
    Set_nickname();

}*/